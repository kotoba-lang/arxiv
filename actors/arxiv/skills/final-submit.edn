{:skill/id :arxiv.final-submit
 :skill/uses :yorishiro/arxiv
 :skill/risk :public-submit
 :skill/params [:submission-id]
 :skill/requires [:human-approval]
 :skill/routes
 [{:route/id :human-handoff
   :steps [{:do :human-approval
            :reason "Final arXiv submission creates a public scholarly record."}
           {:do :screenshot :as "before-final-submit"}
           {:do :click :match {:text "Submit"}}
           {:do :checkpoint :id :submitted}]}]}

