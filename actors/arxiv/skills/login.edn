{:skill/id :arxiv.login
 :skill/uses :yorishiro/arxiv
 :skill/risk :external-auth
 :skill/params []
 :skill/doc
 "Log in at https://arxiv.org/login. Prefer computer route (Playwright runner).
  Secrets: 1Password Private vault item 'Arxiv - N24' for junkawasaki-n24y
  (n24y001j@mail.cc.niigata-u.ac.jp)."
 :skill/routes
 [{:route/id :computer
   :prefer 1
   :steps [{:do :run
            :argv ["python" "-m" "arxiv_submit"
                   "--package" [:param :package-dir "submissions/sqrt-space-kv"]
                   "--stop-after" "login"]
            :env {:ARXIV_USER {:secret :arxiv/user}
                  :ARXIV_PASSWORD {:secret :arxiv/password}}}]}
  {:route/id :browser-dom
   :prefer 50
   :steps [{:do :navigate :url "https://arxiv.org/login"}
           {:do :fill :match {:css "input[name=username]"} :value {:secret :arxiv/user}}
           {:do :fill :match {:css "input[name=password]"} :value {:secret :arxiv/password}}
           {:do :click :match {:role "button" :name "Submit"}}
           {:do :checkpoint :id :logged-in}
           {:do :screenshot :as "arxiv-logged-in"}]}
  {:route/id :human-handoff
   :prefer 100
   :steps [{:do :wait-human :prompt "Log in to arXiv as junkawasaki-n24y, then resume."}]}]}
